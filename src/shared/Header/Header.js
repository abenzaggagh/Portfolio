import React from "react";

import './Header.scss';

export default class Header extends React.Component {

    render() {
        return (
        <header>
            <div className="icon">
                <span></span>
            </div>
        </header>);
    }
}